---
interface Props {
  emoji?: string;
}

const { emoji = '😱' } = Astro.props;
---

<aside class="bg-orange-200 dark:bg-gray-900 p-4 flex items-center justify-between gap-2 my-2">
  <div>
    <slot />
  </div>
  <p class="relative">
    <span class="absolute animate-ping">{emoji}</span>
    {emoji}
  </p>
</aside>